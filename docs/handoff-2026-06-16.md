# FruitySplash Handoff - 2026-06-16

This note captures the current work before reset/rest.

## Current Project

- Root: `C:\Apps\FruitySplash`
- Expo SDK: `~56.0.3`
- Android package: `com.hieplex.fruitysplash`
- Google Play app name: `Fruity Splash`

## Important Repo Rule

`C:\Apps\FruitySplash\AGENTS.md` says:

```text
Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.
```

## Implemented Recently

### Gameplay Audio

- Added background music loop from `assets/GamePlay`.
- Added booster sound effects:
  - LightningFruits sound when `LightningComeDown.png` comes down.
  - Bomb sound when `BombExploded.png` shows.
  - LineRocket sound when `LineRocketImg.png` shows.
  - FruityCross sound when the 4 pieces split out.
- Replaced match sound with `assets/fruity/VFX/Matched.mp3`.
- Match sound is used for match 3, 4, 5, 7 and special merge creation.

### Boosters and Match Cells

- Hammer button is hidden/removed for now.
- Special fruit images are wired for Apple, Grape, Strawberry, Orange, Blueberry.
- Special cell image mapping is based on fruit + special type:
  - horizontal / vertical for match 4
  - cross for match 5/6/7 style cells
- Old white circle special-cell visual was removed.
- Fixed behavior where matching a merged/special cell in a match-3 should trigger the special cell effect instead of only normal match-3.
- Asset preloading/runtime wiring was updated for fruits, boosters, match effects, and finish screen assets.

### Finish Screen

- Old finish panel/text/buttons removed.
- Finish background replaced.
- `FinishBoard.png` centered and sized.
- Finish stars added:
  - Uses `FinishStar` and `UnfinishStar`.
  - Shows star count from gameplay result.
  - Golden star reveal fills upward from bottom to top.
- Continue button added from `assets/FinishScreen/ContinueButton.png`.
- Coin reward display added below stars:
  - Coin image.
  - Reward number rendered with digit images from `assets/FinishScreen/RewardNumbers`.
  - Reward number image files were normalized/renamed as `0.png` through `9.png`.
- Gameplay test tuning:
  - Star thresholds restored to 25%, 50%, 100%.
  - Move count/debug finish settings were adjusted during testing; check `app/level/[id].tsx` before release.

### Map Star Updates

- Finish Continue now routes to `/map` with:
  - `animateLevel`
  - `stars`
- Map now focuses the socket for the level just played, not the newly unlocked/next band.
- Map socket stars now render saved golden stars.
- The played level's map stars animate after the loading wipe.
- Map socket golden star reveal now fills upward from bottom to top, matching finish-screen style.
- Fixed React Native Animated mixed-driver crash by separating JS-driven fill height and native-driven pop scale.

### Google Account Login

- Installed `@react-native-google-signin/google-signin`.
- Added config plugin in `app.json`.
- Added `src/state/google-auth.tsx`.
- Wrapped app with `GoogleAuthProvider` in `app/_layout.tsx`.
- Home screen has a small Google sign-in button.
- Map screen replaced old return button with an account/avatar button.
- Account/avatar button:
  - Uses Google profile photo when signed in.
  - Shows `G` fallback when not signed in.
  - Uses downloaded frame image at `assets/Avatar/AvatarFrame.png`.
- Added `src/components/avatar-frame-button.tsx`.
- Added Google setup note at `docs/google-sign-in.md`.

## Current Save System

Progress is still local only.

- File: `src/state/progress-store.ts`
- Storage: `AsyncStorage`
- Key: `fruity-splash-progress-v1`
- Saved locally:
  - stars
  - unlocked level
  - best scores
  - coins
  - boosters
  - lives
  - sound setting

Google login currently provides identity only. It does not cloud-save progress yet.

## Supabase Plan

Supabase plugin/tooling is available in the Codex session, but Supabase sync has not been implemented yet.

Recommended next steps:

1. Create or choose a Supabase project.
2. Add a table for user saves, for example `player_saves`.
3. Store rows keyed by Google user id or Supabase auth user id.
4. Add RLS policies so users can only read/write their own save.
5. Install `@supabase/supabase-js`.
6. Add env vars:

```powershell
$env:EXPO_PUBLIC_SUPABASE_URL="https://YOUR_PROJECT.supabase.co"
$env:EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY="YOUR_PUBLISHABLE_KEY"
```

7. Add a cloud sync layer that:
   - loads local progress first for offline play
   - after Google/Supabase login, downloads cloud save
   - merges using best stars, best scores, max coins/valid inventory
   - uploads local progress after changes

Important: never put a Supabase `service_role` key in the app.

## Build Notes

For Google Play `.aab`:

```powershell
cd C:\Apps\FruitySplash
npx eas-cli@latest login
npx eas-cli@latest build:configure
npx eas-cli@latest build -p android --profile production
```

For Google Sign-In testing/builds:

```powershell
$env:EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID="YOUR_WEB_CLIENT_ID.apps.googleusercontent.com"
npx eas-cli@latest build -p android --profile production
```

Google Sign-In will not work in Expo Go; it needs a native build/dev client.

## Verification Last Run

Last successful check:

```powershell
npm run typecheck
```

Result: passed.

## Watch Items Before Release

- Check `app/level/[id].tsx` for debug move count / finish target values before publishing.
- Supabase cloud save is not implemented yet.
- Google OAuth setup still needs Play Console/App Integrity SHA-1 and Web Client ID.
- `npm install @react-native-google-signin/google-signin` added dependency tree changes in `package-lock.json`.
- npm audit reported existing vulnerabilities; do not run broad `npm audit fix --force` without review.
